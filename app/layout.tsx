import type { Metadata } from "next";
import "./globals.css";
import Navbar from "./components/Navbar";
import Footer from "./components/Footer";

export const metadata: Metadata = {
  title: "Cecilia Learning Academy | Excellence from Crèche to Secondary",
  description: "Nigeria's premier private school offering world-class education from crèche through secondary. Cecilia Learning Academy — where brilliance begins.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        {children}
        <Footer />
      </body>
    </html>
  );
}
